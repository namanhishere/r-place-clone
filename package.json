{
  "name": "r-place-clone",
  "version": "1.0.0",
  "description": "AWS Serverless backend for r-place-clone",
  "main": "src/handler.js",
  "scripts": {
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.844.0"
  }
}
